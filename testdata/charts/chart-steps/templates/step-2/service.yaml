apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-steps.fullname" . }}
  labels:
    {{- include "chart-steps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-steps.selectorLabels" . | nindent 4 }}
